APP_ENV=local

# --- Local (MySQL) ---
# ローカル開発でも MySQL を利用します。
# いずれか一方の方法で接続情報を設定してください。

# 1) DATABASE_URL を直接指定する場合
# DATABASE_URL=mysql+pymysql://local_user:local_password@localhost:3306/yorizo?charset=utf8mb4

# 2) DB_* 環境変数から組み立てる場合
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=local_user
DB_PASSWORD=local_password
DB_NAME=yorizo

# --- Azure Production/Staging (MySQL + SSL) ---
# APP_ENV=production
# DB_HOST=your-mysql-host
# DB_PORT=3306
# DB_USERNAME=your-db-user
# DB_PASSWORD=your-db-password
# DB_NAME=yorizo
# DB_SSL_CA=/etc/ssl/certs/DigiCertGlobalRootG2.crt.pem

# Azure OpenAI（ローカル動作したい場合は値を入れてください）
AZURE_OPENAI_ENDPOINT=https://aoai-10th.openai.azure.com/
AZURE_OPENAI_API_KEY=your_azure_key_here
AZURE_OPENAI_CHAT_DEPLOYMENT=gpt-5-mini
AZURE_OPENAI_API_VERSION=2024-02-15-preview
# フォールバックで AZURE_OPENAI_DEPLOYMENT も読み取ります

RAG_PERSIST_DIR=./rag_store
ENABLE_RAG=true

CORS_ORIGINS=http://localhost:3000,https://arimakinen-or-die-app-frontend-encsefebejdxdqav.canadacentral-01.azurewebsites.net
